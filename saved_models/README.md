# Saved models

| File | Created by | Purpose |
|------|------------|---------|
| `best_model.keras` | `ModelCheckpoint` (monitors `val_accuracy`) | Best weights during training |
| `asirra_cats_dogs_final.keras` | `--stage save` | Final exported model |

This folder is gitignored once models are trained.
