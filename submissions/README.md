# Kaggle submissions (no labels)

| File | Source | Purpose |
|------|--------|---------|
| `sample_submission.csv` | Kaggle template (or repo placeholder) | Column format reference |
| `asirra_predictions.csv` | Generated by `--stage predict` | Model predictions on unlabeled test |

## Rules

- Predictions use **`data/raw/asirra/test/`** only (unlabeled Kaggle images).
- **Never** used for training, validation, or accuracy reporting.
- Replace `sample_submission.csv` with the official Kaggle file when available.

```bash
python src/app.py --stage predict
```
